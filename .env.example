# Публичные переменные — попадают в бандл, секретов тут быть не должно
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Необязательные (есть значения по умолчанию)
# NEXT_PUBLIC_SUPABASE_CONTENT_TABLE=site_content
# NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=site-assets

# Токен Telegram-бота живёт ТОЛЬКО на хостинге в config.php выше корня сайта
# (см. public/send.php), в этот файл и в git его не класть.
